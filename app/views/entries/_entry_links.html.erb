<!--応募ボタンの表示-->
<% if project.user != current_user %>
    <% if project.entried_by? current_user %>
    応募中
    <% else %>
        <%= link_to "応募", project_entries_path(project_id: project.id, user_id: current_user.id), method: :post, :id => "entries_ajax_#{project.id}", :class => "", remote: true %>
    <% end %>
<% end %>