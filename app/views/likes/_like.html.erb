<!-- ココ追記 カウントのため -->
<h3>いいね件数: <%= @project.likes.count %></h3>

<% if current_user.already_liked?(@project) %>
  <%= button_to 'いいねを取り消す', project_like_path(@project), method: :delete %>
<% else %>
  <%= form_for [@project, @like] do |f| %>
    <!--　エラーメッセージ  -->
    <% if @like.errors.any? %>
      <% @like.errors.full_messages.each do |msg| %>
        <li style="color: red;"><%= msg %></li>
      <% end %>
   <% end %>
    <%= f.submit 'いいね！' %>
  <% end %>
<% end %>

 <h2>このプロジェクトにいいねしたユーザー</h2>
<% @project.liked_users.each do |user| %>
  <li><%= user.user_name %></li>
<% end %> 