<% if project.picked_by? current_user %>
    <%= link_to "pickをやめる", project_picks_path(project.id), method: :delete, :id => "picks_ajax_#{project.id}", :class => "", remote: true %>
<% else %>
    <%= link_to "pick", project_picks_path(project.id), method: :post, :id => "picks_ajax_#{project.id}", :class => "", remote: true %>
<% end %>